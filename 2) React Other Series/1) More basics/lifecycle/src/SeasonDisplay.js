import React from 'react';

export const SeasonDisplay = (props) => {
    return (
        <div>
            <p> Test here: {props.latitude} </p>
        </div>
    );
};

export default SeasonDisplay;